const register = (req, res) => {
    res.status(501).json({ message: 'Not Implemented' });
};

const login = (req, res) => {
    res.status(501).json({ message: 'Not Implemented' });
};

const verifyToken = (req, res) => {
    res.status(501).json({ message: 'Not Implemented' });
};

module.exports = {
    register,
    login,
    verifyToken,
};
